"""Framework integrations for colorless.

Each submodule is dependency-free (it imports NO third-party framework), so importing
`colorless.integrations.*` never pulls a heavy dep into the core. The matching runnable
examples in `examples/` show the wiring against the real frameworks (install those yourself).
"""
